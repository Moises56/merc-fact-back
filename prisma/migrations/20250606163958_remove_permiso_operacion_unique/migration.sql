BEGIN TRY

BEGIN TRAN;

-- DropIndex
ALTER TABLE [dbo].[locales] DROP CONSTRAINT [locales_permiso_operacion_key];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
