BEGIN TRY

BEGIN TRAN;

-- DropIndex
ALTER TABLE [dbo].[locales] DROP CONSTRAINT [locales_numero_local_key];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
